[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so deep links and refreshes work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
